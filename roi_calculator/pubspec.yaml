name: roi_calculator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0
go_router: ^14.0.0
flutter_riverpod: ^2.5.0
environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/
